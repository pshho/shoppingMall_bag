<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC ".//mybatis.org/DTD Mapper3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="bag.service.OrdersMapper">
	<insert id="orderInsert" parameterType="ordDTO">
		insert into orders
		(orders_total_price, order_status, merchant_uid, pay_type, member_id, imp_uid, prod_code, prod_count, prd_name, buyer_name,
		buyer_email, buyer_tel, buyer_addr, buyer_postcode, address_msg, orders_reg_date)
		values
		(#{ordersTotalPrice}, #{orderStatus}, #{merchant_uid}, #{payType}, #{memberId}, #{imp_uid}, #{prodCode}, #{prodCount},
		#{prdName}, #{buyerName}, #{buyerEmail}, #{buyerTel}, #{buyerAddr}, #{buyerPostcode},
		#{addressMsg}, #{ordersRegDate})
	</insert>
	
	<select id="orderList">
		select * from orders limit #{start},#{limit}
	</select>
	
	<select id="myPageOrdList" parameterType="map">
		select * from orders where member_id = #{id} limit #{pd.start}, #{pd.limit}
	</select>
	
	<select id="myOrdCnt">
		select count(*) from orders where member_id = #{id}
	</select>
	
	<select id="cancelOrder" resultType="ordDTO">
		select * from orders where merchant_uid = #{merchantUid}
	</select>
	
	<update id="shipChange" parameterType="ordDTO">
		update orders set order_status = #{orderStatus} where merchant_uid = #{merchant_uid}
	</update>
	
	<update id="cancelOrderUpdate" parameterType="ordDTO">
		update orders set order_status = #{orderStatus}, cancel_reason = #{cancelReason} where merchant_uid = #{merchant_uid}
	</update>
</mapper>